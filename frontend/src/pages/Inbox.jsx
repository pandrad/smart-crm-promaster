import { THEME } from "../theme.js";

// Full implementation in Phase E
export function Inbox() {
  return (
    <div style={{ padding: 32, color: THEME.textMuted, fontSize: 14 }}>
      Inbox — em construção (Phase E)
    </div>
  );
}
